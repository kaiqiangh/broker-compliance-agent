services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: broker_compliance
      POSTGRES_PASSWORD: localdev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
